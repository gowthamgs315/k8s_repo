 apiVersion: v1
 kind: ReplicationController
 metadata:
   name: myng-rc
 spec:
   replicas: 4
   selector: 
     app: myng
   template:
      metadata:
        labels:
          app: myng
      spec:
         containers:
         - name: mgng-cont
           image: nginx
           ports:
           - con  tainerPort: 80        